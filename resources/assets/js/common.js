$(document).ready(function () {
    var PiPayment = new function () {
        var html = document.documentElement;
        var height = html.offsetHeight;

        //Toggle
        $('input[type=checkbox]').bootstrapSwitch();
    };
});